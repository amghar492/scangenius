import { FileText, Image as ImageIcon, Sparkles, Download, Settings, Moon, Sun, X, Upload, Check, Play, RotateCcw } from 'lucide-react';

export const Icons = {
  FileText,
  Image: ImageIcon,
  Sparkles,
  Download,
  Settings,
  Moon,
  Sun,
  X,
  Upload,
  Check,
  Play,
  RotateCcw
};

export const MAX_FILE_SIZE_MB = 20;
export const SUPPORTED_FORMATS = ['application/pdf'];
